type Category = {
  title: string;
  icon: React.ReactNode;
  link: string;
};

type Opinion = {
  content: string;
  author: string;
  rating: string;
};

type Advantage = {
  title: string;
  icon: React.ReactNode;
};
